[tool.ruff]
line-length = 88

target-version = "py314"

[tool.ruff.lint]
select = [
    "E",
    "W",
    "F",
    "I",
    "UP",
    "B",
]

ignore = [
    "E501",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]